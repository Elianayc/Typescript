import { FlyingGr0n3 } from "../model/FlyingGr0n3";

/*
export class SurveillanceAdapterService {

    public fetchSurveillanceData(origen: Escrito | FlyingGr0n3 | Ley): Document {

        if (origen instanceof Escrito) {
            return new Document(
                origen.legajo,
                origen.erogacion,
                origen.cuerpo,
                origen.autor
            );
        }

        if (origen instanceof FlyingGr0n3) {
            return new Document(
                origen.expediente,
                origen.lanzamiento,
                origen.contenido,
                origen.nombre
            );
        }

        // Ley
        return new Document(
            origen.folio,
            origen.emision,
            origen.desarrollo,
            origen.firmante
        );
    }
}
*/